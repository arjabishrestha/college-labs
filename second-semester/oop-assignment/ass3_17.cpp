/*Create a class vector that includes a series of float values. Include methods to perform
the following tasks:
a. To create the vector b. To modify the value of a given element
c. To multiply by a scalar value d. To add two vectors
e. To display the vector in the form (10, 20, 30 …)
Write a main function implementing the above vector class using pointers.
This program must illustrate the use of Objects as function arguments and return type.*/
#include<iostream>
using namespace std;
class vector
{
    float a[20];
public:
    void CreateVector()
    {
        int n;
        cout<<"number of elements in the vector?"
        cin>>n ;
        cout<<"enter"<<n<<"elements";
        for(i=0;i<n;i++)
            cin>>a[n];
    }
    void modify()
    {

    }
    void multiply()
    {

    }
    void add()
    {

    }
    void display(int n)
    {
        cout<<"(";
        for (int i=)
    }
};
